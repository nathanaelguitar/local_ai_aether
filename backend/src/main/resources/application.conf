ktor {
  deployment {
    host = ${?AETHER_HOST}
    host = "0.0.0.0"
    port = ${?AETHER_PORT}
    port = 8787
  }
  application {
    modules = [ com.nathanael.aether.backend.MainKt.module ]
  }
}
